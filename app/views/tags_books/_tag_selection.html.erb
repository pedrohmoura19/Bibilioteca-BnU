<%= form_with(model: @tags_book) do |form| %>
    <% if @tags_book.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@tags_book.errors.count, "error") %> prohibited this tags_book from being saved:</h2>
  
        <ul>
          <% @tags_book.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="row pl-4 py-4">
      <h1>Selecione a Tag que será adcionada ao Livro:</h1>
    </div>
  
    <div class="row pl-5 pb-4">
      <div class="col-3">
        <%= form.label :tag_id %>
        <%= form.select :tag_id, @tags.collect { |tag| [tag.name, tag.id] } %>
      </div>
    </div>
  
    <div class="field">
      <%= form.hidden_field :book_id, value: params[:book_id] %>
    </div>
  
    <div class="row pl-4 pb-4">
      <div class="col-3">
        <%= form.submit "Adcionar tag ao Livro", class:"btn" %>
      </div>
    </div>

 <% end %>
