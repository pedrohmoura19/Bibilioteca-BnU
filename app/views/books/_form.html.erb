<%= form_with model: book do |form| %>
    <div>
      <%= form.label :title, "Título do Livro" %><br>
      <%= form.text_field :title %>
      <% book.errors.full_messages_for(:title).each do |message| %>
          <div><%= message %></div>
      <% end %>
    </div>
  
    <div>
      <%= form.label :author, "Autor" %><br>
      <%= form.text_field :author %>
      <% book.errors.full_messages_for(:author).each do |message| %>
          <div><%= message %></div>
      <% end %>
    </div>
  
    <div>
    <%= form.label :year, "Ano de Publicação" %><br>
    <%= form.number_field :year %>
    <% book.errors.full_messages_for(:year).each do |message| %>
      <div><%= message %></div>
    <% end %>
    </div>
  
  
    <div>
      <%= form.submit "Adcionar Livro" %>
    </div>
  <% end %>